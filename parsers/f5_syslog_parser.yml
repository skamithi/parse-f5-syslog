vars:
  sysloghost:
    name: "{{ item[0] }}"
    ip: "{{ item[1] }}"
keys:
  sysloghosts:
    value: "{{ sysloghost }}"
    start_block: "^sys syslog {"
    end_block: "^}$"
    items:
      - "\\s{6,}(.+) {"
      - "\\s+host (.+)"

